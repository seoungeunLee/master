import React from 'react';

const TestButton = () => {
  return (
    <button className="block rounded-md bg-[mistyrose] px-4 py-2 text-[#333]">
      해당 btn 컴포넌트는 클릭하면 사라집니다.
    </button>
  );
};

export default TestButton;
